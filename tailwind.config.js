import defaultTheme from 'tailwindcss/defaultTheme'

export default {
    theme: {
        extend: {
            fontFamily: {
                head: ['Space Grotesk', 'sans-serif'],
            }
        }
    }
}